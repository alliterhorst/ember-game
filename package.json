{
  "name": "ember",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "EMBER — uma centelha que reacende um mundo adormecido. Web game 3D (mobile + desktop).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "howler": "^2.2.4",
    "three": "^0.171.0"
  }
}
